[user]
	email = lam.dev.hs@gmail.com
	name = lamdevhs
[core]
  editor = vim
[alias]
	cached = diff --cached
	st = status -s
	graph = show-branch
	last = log -1
	past = log -1 -p
	line = log --oneline -3
	liness = log --oneline
	lines = log --oneline -6
	stashed = stash list
	cfg = config --global
	append = add -v
	vu = add -vu
	amend = commit --amend
	aka = ! git config --global --list | grep 'alias' | grep -v aka | sed 's/^alias\\.//g' | sed 's/=/\\t|\\t/g'
